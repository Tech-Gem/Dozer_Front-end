import 'package:flutter/material.dart';

class BidScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bid Screen'),
      ),
      body: Center(
        child: Text('This is the Bid Screen'),
      ),
    );
  }
}

class AllMachinesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('All Machines Screen'),
      ),
      body: Center(
        child: Text('This is the All Machines Screen'),
      ),
    );
  }
}

class BookingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bookings Screen'),
      ),
      body: Center(
        child: Text('This is the Bookings Screen'),
      ),
    );
  }
}
